<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.siyan1234.itproject2nd.member.dao.MemberDao">

    <select id="findByMemberId" parameterType="String" resultType="MemberDto">
        SELECT no,
        member_id,
        password,
        name,
        nickname,
        email,
        phone,
        birth_date,
        gender,
        postcode,
        address,
        detail_address,
        role,
        profile_image,
        last_login_date,
        agree_terms_yn,
        agree_privacy_yn,
        regdate
        FROM member
        WHERE member_id = #{memberId}
        <!-- #{memberId}는 Dao에서 @Param("memberId")로 넘긴 값 -->
    </select>

    <select id="findByEmail" parameterType="String" resultType="MemberDto"> <!-- 이메일 중복 확인용 -->
        SELECT no,
        member_id,
        password,
        name,
        nickname,
        email,
        phone,
        birth_date,
        gender,
        postcode,
        address,
        detail_address,
        role,
        profile_image,
        last_login_date,
        agree_terms_yn,
        agree_privacy_yn,
        regdate
        FROM member
        WHERE email = #{email}
        <!-- 이메일이 이미 존재하는지 확인 -->
    </select>

    <insert id="signup" parameterType="SignupDto">
        <!-- id는 MemberDao의 signup 메서드명과 일치해야 함. / parameterType은 회원가입 폼 값을 담은 SignupDto -->
        INSERT INTO member (
        member_id,
        password,
        name,
        nickname,
        email,
        phone,
        birth_date,
        gender,
        postcode,
        address,
        detail_address,
        role,
        agree_terms_yn,
        agree_privacy_yn
        )
        VALUES
        (
        #{memberId},
        #{password},
        #{name},
        #{nickname},
        #{email, jdbcType=VARCHAR},
        #{phone, jdbcType=VARCHAR},
        #{birthDate, jdbcType=DATE},
        #{gender, jdbcType=VARCHAR},
        #{postcode, jdbcType=VARCHAR},
        #{address, jdbcType=VARCHAR},
        #{detailAddress, jdbcType=VARCHAR},
        'USER',
        #{agreeTermsYn},
        #{agreePrivacyYn}
        )

    </insert>


    <!-- ========================= -->
    <!-- 로그인 -->
    <!-- ========================= -->
    <!--
        MemberDao.login(LoginDto loginDto) 와 연결됨

        입력한 아이디 / 비밀번호가 DB와 일치하고
        탈퇴 처리되지 않은 회원만 조회함
    -->
    <select id="login"
            parameterType="com.siyan1234.itproject2nd.member.dto.LoginDto"
            resultType="com.siyan1234.itproject2nd.member.dto.MemberDto">

        SELECT
        NO,
        MEMBER_ID,
        PASSWORD,
        NAME,
        NICKNAME,
        EMAIL,
        PHONE,
        BIRTH_DATE,
        GENDER,
        POSTCODE,
        ADDRESS,
        DETAIL_ADDRESS,
        ROLE,
        AGREE_TERMS_YN,
        AGREE_PRIVACY_YN,
        REGDATE
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
        AND PASSWORD = #{password}


    </select>


    <!-- ========================= -->
    <!-- 아이디 중복확인 -->
    <!-- ========================= -->
    <!--
        MemberDao.countByMemberId(String memberId) 와 연결됨

        결과가 0이면 사용 가능
        결과가 1 이상이면 이미 사용 중
    -->
    <select id="countByMemberId"
            parameterType="string"
            resultType="int">

        SELECT COUNT(*)
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}

    </select>


    <!-- ========================= -->
    <!-- 이메일 중복확인 -->
    <!-- ========================= -->
    <!--
        MemberDao.countByEmail(String email) 와 연결됨

        이메일은 선택값이지만,
        입력했을 때 이미 존재하면 회원가입을 막기 위해 사용
    -->
    <select id="countByEmail"
            parameterType="string"
            resultType="int">

        SELECT COUNT(*)
        FROM MEMBER
        WHERE EMAIL = #{email}

    </select>

</mapper>